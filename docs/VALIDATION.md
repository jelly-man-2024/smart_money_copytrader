# 初版验证记录

本文保留 2026-09-09 初版证据。服务器上的后续 M2 与纸面跟单验证见
[SERVER_VALIDATION_2026-09-11.md](SERVER_VALIDATION_2026-09-11.md) 和
[SERVER_VALIDATION_2026-09-12.md](SERVER_VALIDATION_2026-09-12.md)；当前测试数和能力边界以
后两份记录及实际代码为准。

日期：2026-09-09。Python 3.10.12，Linux x86_64。

## 离线

- 已安装独立虚拟环境，直接依赖固定版本，完整已测试解析保存在 requirements.lock。
- unittest：46 项通过。包括 legacy/type 1/2/4 本地验签、Nitro 嵌套/畸形输入、
  错链、过期/未来消息、缺口/重复、假 Router/EntryPoint、未知委托、claim + swap、
  UserOp 失败和日志隔离、数据库去重、RPC 广播拒绝、URL 错误信息去密。
- 真实样本回归：CRIBS 直接买入和自身批量卖出、RIPE 领奖/转账、LP、
  397397 的 USDG 存款、第三方交付和 230 地址分发。
- 离线 replay：11 笔历史 + 1 笔 feed + 1 笔批量分发，共 13 笔。
- 分发只产生一条汇总，列出 230 个总收款人和 32 个清单收款人数量；不是 32 个买单。
- 所有输出 copy_eligible=false；未读取用户私钥、未提交真实交易。

## 15 秒实时只读 smoke

公开 feed 和 RPC 已连通。一次有限时运行记录：

- 600 帧，跳过 4,988 笔过期回放，解码 625 笔新鲜交易。
- 命中 1 笔候选并获取回执，没有队列丢弃或 worker 错误计数。
- 该笔 0x1220097a9578d0ca3b43d466e22615e72fbab0ee1b521d1243578ecb7dfe98a3
  被保守标为 EXTERNAL_DELIVERY_CANDIDATE，非自动买单。
- 该运行验证接入和候选回执链路，不证明所有路由覆盖、延迟 SLA、最终性或收益。

在网络受限沙箱中，Python 线程唤醒测试曾挂起；同一测试在允许的环境下通过。
随后将错误信息去密单测改为直接测试同步传输，并单独 mock 异步派发；46 项现在在
受限沙箱中也全部通过。真实网络只读 smoke 经权限流程执行。挂起的测试进程已中止。

## 尚不能宣称完成

没有执行模拟成交或实盘；没有做持续规范链重查、订单归属闭环或所有代币行为验证。
UNKNOWN/needs_review 是显式未支持的情况，不能把未解出的样本算成已确认兑换。
