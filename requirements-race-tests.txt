# Optional offline EVM tests only. Install --no-deps into a separate --target
# directory alongside the already installed project requirements, never live .venv.
py-evm==0.12.1b1
eth-bloom==3.1.0
trie==3.1.0
lru-dict==1.3.0
cached-property==2.0.1
sortedcontainers==2.4.0
