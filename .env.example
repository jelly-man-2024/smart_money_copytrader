# Optional read-only endpoints. Copy to .env; only these two names are loaded.
# Wallet/private-key variables are deliberately ignored.
ROBINHOOD_RPC_URL=https://rpc.mainnet.chain.robinhood.com
ROBINHOOD_FEED_URL=wss://feed.mainnet.chain.robinhood.com
